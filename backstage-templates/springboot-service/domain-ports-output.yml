apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: springboot-domain-ports-output
  description: Output ports (repository interfaces) for Spring Boot template
  tags:
    - domain
    - ports
    - output
    - repositories
    - hexagonal-architecture
  annotations:
    github.com/project-slug: addon-ai/backstage-templates
spec:
  type: library
  owner: platform-team
  lifecycle: production
  subcomponentOf: component:default/springboot-domain-layer
