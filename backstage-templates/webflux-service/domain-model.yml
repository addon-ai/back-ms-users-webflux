apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: webflux-domain-model
  description: Domain models and entities for Spring WebFlux template
  tags:
    - domain
    - model
    - entities
    - hexagonal-architecture
    - reactive
  annotations:
    github.com/project-slug: addon-ai/backstage-templates
spec:
  type: library
  owner: platform-team
  lifecycle: production
  subcomponentOf: component:default/webflux-domain-layer
