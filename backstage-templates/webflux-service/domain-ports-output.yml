apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: webflux-domain-ports-output
  description: Output ports (repository interfaces) for Spring WebFlux template
  tags:
    - domain
    - ports
    - output
    - repositories
    - hexagonal-architecture
    - reactive
  annotations:
    github.com/project-slug: addon-ai/backstage-templates
spec:
  type: library
  owner: platform-team
  lifecycle: production
  subcomponentOf: component:default/webflux-domain-layer
