-- PostgreSQL initialization script for {{project.general.name}}
-- This script runs automatically when the container starts for the first time

-- Create extensions if needed
CREATE EXTENSION IF NOT EXISTS "uuid-ossp";
CREATE EXTENSION IF NOT EXISTS "pgcrypto";

-- Grant permissions
GRANT ALL PRIVILEGES ON DATABASE {{project.general.name}}_db TO postgres;

-- Log initialization
\echo 'Database {{project.general.name}}_db initialized successfully'