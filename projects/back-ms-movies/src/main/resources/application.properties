# Application Configuration
spring.application.name=back-ms-movies
spring.profiles.active=local

# Default profile includes local development settings
# Use specific profiles for different environments:
# - local: Local development with hardcoded values
# - develop: develop environment with environment variables
# - test: test environment with environment variables
# - staging: staging environment with environment variables
# - main: main environment with environment variables
