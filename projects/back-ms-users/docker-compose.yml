version: '3.8'

services:
  postgres:
    image: postgres:15.0
    container_name: back-ms-users-postgres
    environment:
      POSTGRES_DB: back_ms_users_db
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password123
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - back-ms-users-network

  back-ms-users-service:
    build: .
    container_name: back-ms-users-service
    environment:
      SPRING_DATASOURCE_URL: jdbc:postgresql://postgres:5432/back_ms_users_db
      SPRING_DATASOURCE_USERNAME: postgres
      SPRING_DATASOURCE_PASSWORD: password123
    ports:
      - "8080:8080"
    depends_on:
      - postgres
    networks:
      - back-ms-users-network

volumes:
  postgres_data:




networks:
  back-ms-users-network:
    driver: bridge